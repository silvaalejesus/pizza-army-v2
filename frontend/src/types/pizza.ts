export interface IPizza {
  flavor: string;
  description: string;
  price: number;
  size: string;
  slices: number;
  fotoUrl: string;
  characteristics: string;
  id: string;
  quantity: number;
}
